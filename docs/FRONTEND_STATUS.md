# 프론트엔드 1차 구현 완료 (2026-09-12)

Phase 7(배포)보다 프론트엔드를 먼저 하기로 한 결정에 따라, **구현된 백엔드가 있는 화면만**
만들었다. 목표는 완성도 높은 제품이 아니라 Phase 3~6의 결과를 눈으로 확인하는 것이다.

## 만든 화면

| 화면 | 목업 | 상태 |
|---|---|---|
| 로그인 | 1i | GitHub OAuth 단일 경로, HttpOnly 쿠키 |
| 프로젝트 목록 | 1c | 표 ↔ 보드 전환, 필터 레일, 커서 페이지네이션 |
| 프로젝트 개요 | 1d | 탭 7개, 환경 시각화(서비스 카드), 헬스 4지표 |
| 환경 구성 + Policy Gate | 1e | 생성·승인·반려, 차단 계약 |
| 대시보드 | 1a | KPI 5, 진행 상황, SSE 스트림, DORA |
| 리포트 | 1f | 추이선, 비용, 실행 분포, 통과율 |
| 설정 | 1h | 예산·API 키·Git 연동 |

## 만들지 않은 것과 이유

- **인박스(1g)·감사 로그** — 설계서 Part 4 §8이 미구현이다. `AUDIT_LOGS`는 엔티티만 있고
  쓰는 곳도 읽는 곳도 없다. 화면만 먼저 만들면 빈 목록을 "알림이 없다"로 보여 주게 되는데
  그건 사실이 아니라 거짓말이다.
- **1b(상단 내비 + 칸반)** — 설계서가 셸을 1a로 확정하며 칸반만 목록의 보기 전환으로
  흡수했다. 칸반은 프로젝트 목록의 `?view=board`에 있다.
- **칸반 드래그 앤 드롭** — "카드를 옮기면 단계 전환이 기록된다"는 설계서 문구가 있으나
  이번 범위(기능 확인)를 넘는다.
- **설정의 알림 탭** — 저장할 백엔드가 없다. 동작하지 않는 폼은 고장으로 보인다.
- **멤버 탭** — 자리만 두고 "확장" 배지. 설계서 09가 "지금 만들면 쓸 사람이 없다"고 정했다.

## 두 자료의 충돌을 어떻게 다뤘나

`Muster Console.dc.html`(목업 1a~1j)은 **다크 Nocturne + 보라 액센트**이고,
UI/UX 설계서 v2.1은 그 색 체계를 **명시적으로 폐기**하고 잉크 그레이스케일 + 단일 신호색으로
바꿨다. 사용자 결정: **구조·정보 밀도는 목업, 색·타이포·배지 규칙은 설계서.**

## 설계서에서 발견한 오류

**다크 신호색 위 흰 글자가 안 읽힌다.** 설계서는 "배지의 채운 신호색 위 흰 글자가 가장
빠듯하다. 다크 매핑에서 신호색을 올린 이유이기도 하다"고 적었는데 **거꾸로다** — 색을 밝게
올리면 흰 글자 대비는 나빠진다. WCAG 공식으로 직접 계산한 값:

| 조합 | 실측 | 판정 |
|---|---|---|
| 라이트 `#8E1B1B` 위 흰 글자 | 9.04:1 | AAA |
| 다크 `#D98A8A` 위 흰 글자 | **2.64:1** | **미달** |
| 다크 `#D98A8A` 위 잉크 글자 | 6.86:1 | AA ✓ |

`--color-on-signal` 토큰을 신설해 다크에서 글자를 잉크로 뒤집었다. 설계서의 대비표 자체도
실측과 1~2포인트씩 다르다(본문/바탕 라이트 15.8 표기 → 실측 17.63).

**간격 규칙이 자기모순이다.** "4의 배수만 쓴다"고 선언하고 카드 내부 여백을 9로 적었다.
규칙이 개별 수치보다 상위라고 보고 12로 올렸다(8로 내리면 카드 사이 간격 8과 같아져
"여백 > 카드 간격" 관계가 무너진다).

**타입·간격 수치의 단위계가 명시돼 있지 않다.** 본문 6.2pt는 포스터 스케일 캔버스 값이라
웹에서 약 8px이 된다. 비율만 가져와 계수 k = 14px / 6.2pt = 2.2581로 환산했다
(25/17/14/12/11px). 시안 실측으로 맞추는 것이 남아 있다.

## 백엔드에 추가한 것

| 추가 | 이유 |
|---|---|
| `GET /projects/:id/git-integration` | 설계서 §3에 POST·DELETE만 있고 GET이 없다. 화면이 레포를 읽을 방법이 없었다 |
| `run_outcomes.tokens_used` | 대시보드 "오늘 토큰" KPI에 대응 집계가 없었다 |
| `GET /reports/summary` | 리포트 화면 전체. 클라이언트가 AGENT_RUNS를 긁어 합산하면 numeric을 부동소수점으로 더하게 된다 |
| HttpOnly 쿠키 인증 | EventSource가 헤더를 실을 수 없어 SSE에 Bearer를 쓸 수 없다 |

## 실행하고 나서야 잡힌 버그들

정적 검사로는 안 잡히고 **실제로 돌려야만** 드러난 것들이다.

1. **`apiFetch`가 204에서 터졌다.** 본문 없는 응답에 `res.json()`을 부르면 SyntaxError로
   죽는데, 그때 서버에서는 이미 삭제가 끝나 있다. 키 폐기·연동 해제가 "실패했다"고 뜨면서
   실제로는 지워지는 상태였다.
2. **`null` 반환이 본문 없는 200을 만든다.** 컨트롤러가 null을 돌려주면 Nest가 빈 본문을
   보내고 클라이언트가 거기서 터진다. `{ integration: null }`로 감쌌다.
3. **셸이 가로로 밀렸다.** 플렉스 자식의 기본 `min-width: auto`가 `<input>`의 고유 너비로
   잠겨 좁은 화면에서 상단바가 뷰포트보다 넓어졌다.
4. **e2e 스위트가 두 페이즈에 걸쳐 빨간 상태였다.** 단위 테스트만 돌리고 완료를 보고해 온
   탓이다. CHECK 제약 수는 Phase 4부터, 엔티티·테이블 수는 Phase 6부터 어긋나 있었다.

## 검증 상태 (2026-09-12)

- API: `tsc` exit 0, 단위 **245**, e2e **101**, lint 기존 14건(신규 0)
- Web: 빌드 성공, lint 0, 테스트 **5**
- 브라우저: 라이트/다크 양쪽, 로그인·목록·개요·대시보드·리포트·설정·환경구성 전부 실데이터로 확인

## 남은 것

- **Part 4 §8(감사 로그)** — 인박스·감사 탭의 전제
- **API 키 말미 표기** — `PROJECT_API_KEYS`에 접두·말미가 없어 목업의 `msk_••••4f2a`를
  만들 수 없다. `key_suffix` 컬럼이 필요하다
- **환경 구성 상태 전이 이력** — `build_status`가 단일 컬럼이라 "어떤 경로로 왔는가"를
  역산하고 있다. 재생성·반려를 거친 이력은 드러나지 않는다
- **`POST /agents/:id/runs`가 브라우저에서 불가** — `@Public + ApiKeyGuard`라 세션으로는
  401이다. 개요 화면의 "에이전트 실행" 버튼이 비활성인 이유다
- **`top_block_reasons`에 식별자 없음** — 리포트에서 해당 구성으로 들어가는 링크를 못 건다
- **공유 컴포넌트 승격** — 카드·표·폼 스타일이 화면마다 복제돼 있다. 여섯 화면이 다 붙었으니
  이제 `app.css`/`components/`로 올릴 시점이다
- **멤버 목록 API 없음** — 목업의 "owner 1" 표기를 못 그린다

## 개발 환경

```bash
docker compose up -d --build                                    # API + DB
docker compose exec -T db psql -U muster -d muster < scripts/seed-dev.sql   # 시드(멱등)
pnpm dev:web                                                    # http://localhost:5173
```
