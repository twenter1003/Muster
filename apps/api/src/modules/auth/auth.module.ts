import { Module } from '@nestjs/common';

/**
 * Auth — 인증, 세션/토큰 관리, (확장 단계) PROJECT_MEMBERS 권한 검사.
 * PRD 3.6 / TechSpec 2장 / API 2장.
 * Phase 3에서 GitHub OAuth 플로우와 SessionResolver 실제 구현을 채운다.
 */
@Module({})
export class AuthModule {}
