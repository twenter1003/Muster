import { Module } from '@nestjs/common';

/**
 * EnvCatalog — 기술스택/환경 템플릿, LLM 도커 설정 생성, Policy Gate 연동.
 * PRD 3.2 / TechSpec 6.1 / API 5장.
 * Phase 5에서 구현. 실행 어댑터는 인터페이스만 두고 구현은 보류(TechSpec 9장).
 */
@Module({})
export class EnvCatalogModule {}
